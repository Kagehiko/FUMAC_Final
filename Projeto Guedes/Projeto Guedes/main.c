#include "automato.h"

void main(void)
{
	automato teste;
	load_file(&teste,"Example.aut");
	/*open_file();
	write_file();
	divide_strings();
	search_strings();
	write_states();
	write_events();
	write_initial_states();
	write_marked_states();
	separate_transitions();
	check_automata();
	check_first_states();
	check_reach_states();
	check_events();
	write_all_function_form();
	check_determin();

	//se tenho um estado em que tenho um mesmo evento a ir para sitios diferentes
	//separar dentro das transições os elementos entre virgulas para um novo vetor
	//dentro das transições comparar se existe para um mesmo estado um evento igual

	free(ptr_one);
	free(p_transitions);
	free(lines);
	free(states);
	free(events);
	free(transitions);
	free(initial);
	free(marked);
	free(transicoes);
	free(transitions_seperated);*/
	getchar();
}